# Normalise to LF. Shell and Node scripts with a CRLF shebang fail on Linux
# with a confusing "bad interpreter" error, and this repo is authored on
# Windows, so leaving it to chance is not an option.
* text=auto eol=lf

# Screenshot baselines are committed artifacts and byte-compared by Playwright.
# Git must never touch them.
*.png binary
