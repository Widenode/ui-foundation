# Every path in this repository is owned by the maintainer team.
#
# This file only REQUESTS review. It becomes a merge requirement when
# "Require review from Code Owners" is enabled in branch protection —
# see CONTRIBUTING.md for the full list of settings to turn on.
#
# The team must exist in the org AND have write access to this repo, or
# GitHub silently treats the rule as unsatisfiable and the protection is
# a no-op with only a warning in the UI.

* @Widenode/developers

# Belt and braces: the files that decide what gets published, what the
# spec is, and who reviews. Listed again so a future narrower rule above
# can never accidentally leave them unowned.
/RULES.md              @Widenode/developers
/package.json          @Widenode/developers
/.github/              @Widenode/developers
/src/tokens.css        @Widenode/developers
